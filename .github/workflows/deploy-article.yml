name: Deploy Article

on:
  workflow_dispatch:
    inputs:
      filename:
        description: 'JSON-Dateiname'
        required: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install Tools
        run: sudo apt-get update && sudo apt-get install -y curl jq

      - name: Download Article JSON from Supabase (Debug)
        env:
          SUPABASE_URL: https://ngtujvgsbymtxphwncuj.supabase.co
          SUPABASE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: |
          mkdir -p work
          filename="${{ github.event.inputs.filename }}"
          echo "Downloading $filename ..."
          curl -v -H "Authorization: Bearer $SUPABASE_KEY" \
            "$SUPABASE_URL/storage/v1/object/articles/$filename" -o work/article.json
          echo "---- Dateiinhalt ----"
          cat work/article.json
          echo "---------------------"
          if [ ! -s work/article.json ]; then
            echo "JSON fehlt oder leer. Abbruch."
            exit 1
          fi
          jq . work/article.json

      - name: Build HTML from template
        run: |
          title=$(jq -r '.title // empty' work/article.json)
          content=$(jq -r '.content // empty' work/article.json)
          preview=$(jq -r '.preview // empty' work/article.json)
          slug=$(echo "$title" | tr '[:upper:]' '[:lower:]' | sed -E 's/[^a-z0-9]+/-/g' | sed -E 's/^-|-$//g')
          filename="beitrag/${slug}.html"
          sed "s/{{TITLE}}/$title/g; s/{{CONTENT}}/$content/g; s/{{PREVIEW}}/$preview/g" template.html > "$filename"
          echo "HTML gebaut: $filename"

      - name: Commit & Push
        env:
          GIT_AUTHOR_NAME: github-actions[bot]
          GIT_AUTHOR_EMAIL: github-actions[bot]@users.noreply.github.com
        run: |
          git config user.name "${GIT_AUTHOR_NAME}"
          git config user.email "${GIT_AUTHOR_EMAIL}"
          git add beitrag/
          git commit -m "Deploy article: $filename" || echo "Nothing to commit"

          git push


